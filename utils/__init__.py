"""
Shared utility helpers used across the bot.
 
Modules:
    messaging  — Functions for sending messages, images, and menus via
                 the Telegram Bot API (``send_text``, ``send_image``, etc.).
    resources  — Functions for loading static text files from the
                 ``resources/`` directory (``load_message``, ``load_prompt``).
"""