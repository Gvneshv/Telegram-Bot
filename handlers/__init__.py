"""
Telegram update handler functions, split by type.
 
Modules:
    commands  — ``CommandHandler`` functions for all slash commands
                (``/start``, ``/gpt``, ``/quiz``, etc.).
    callbacks — ``CallbackQueryHandler`` functions for all inline keyboard
                button presses.
    messages  — ``MessageHandler`` functions for text, voice, and photo
                messages.
"""